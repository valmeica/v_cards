<p><%= link_to "Home", posts_path %></p>

<h1>Edit your entry:</h1>

<%= form_for @post, :html => { :multipart => true } do |f| %>
	<p>
    	<%= f.label :title %>
    	<%= f.text_field :title %>
  	</p>
  	<p>
    	<%= f.file_field :image %>
  	</p>

  	<%= f.submit %>
<% end %>